services:
  frontend:
    container_name: "pokepedia-ui"
    image: pokepedia/frontend:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1513:1513"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    restart: always