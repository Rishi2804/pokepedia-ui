import {styled} from "@mui/system";
import {Box} from "@mui/material";

export const DexHeader = styled(Box)(() => ({
    justifyContent: 'center',
    alignItems: 'center',
    display: "flex",
    flexDirection: 'column',
    paddingBottom: 40,
    paddingTop: 20
}))